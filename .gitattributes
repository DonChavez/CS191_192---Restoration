# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# lways overwrite update.txt with the PR version to avoid merge conflicts.
"Updates/What’s been added" merge=keep-pr
